<?xml version="1.0" encoding="utf-8"?>

<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Ideally I would've used "?colorPrimary" like elsewhere but it fails pre API 23.
         This means we need a separate selector for light and dark themes. -->
    <item android:alpha="0.2" android:color="@color/md_theme_light_primary" android:state_activated="true" />
    <item android:color="@android:color/transparent" />
</selector>
